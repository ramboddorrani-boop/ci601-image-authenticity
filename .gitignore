# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
env/

# Trained model — too large for GitHub, hosted on Google Drive.
# Keep the models/ folder itself in the repo (with .gitkeep) so the
# path exists when someone clones — only the weights are excluded.
models/*.pt
models/*.pth
!models/.gitkeep

# User uploads at runtime
static/uploads/

# Editor
.vscode/
.idea/
*.swp

# OS / misc
Thumbs.db
*.log
